# 使用混合岗位匹配引擎

岗位匹配先以确定性规则判断资格门槛，再由模型依据画像证据评估技能、项目和职业方向。系统保存分项得分、证据引用、缺口、否决项以及模型和提示词版本，不采用只返回单一总分的纯大模型判断；这样牺牲了一部分实现简洁性，换取可解释、可复现和可校准的推荐结果。

## Considered Options

- 纯规则评分：稳定但难以理解技能迁移、项目深度和自然语言岗位要求。
- 纯模型评分：实现直接，但结果波动、证据不透明且难以回归测试。
- 混合评分：以规则约束可行性，以模型处理语义判断。
