# 使用受约束的 Agent 工作流

Agent 运行遵循可持久化流程、显式状态转移、工具白名单、成本预算和终止条件；模型只在指定步骤内进行语义判断、制定局部计划或选择允许的下一步。系统保留观察、规划、行动和反馈闭环，但不允许模型自由扩张任务范围或绕过用户审批执行外部行动。

## Considered Options

- 开放式 Agent：灵活但难以测试、复现和控制成本与副作用。
- 固定自动化脚本：稳定但无法处理自然语言岗位与用户差异。
- 受约束工作流：把确定性生命周期与模型语义能力结合。
