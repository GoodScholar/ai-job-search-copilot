# 使用可持久化的 Agent 运行

岗位发现、批量匹配和材料生成将作为可追踪的后台 Agent 运行执行，而不是绑定在单次长时间 HTTP 请求上。每次运行具有明确输入输出、进度和终态，支持重试、取消、失败恢复，并能在等待用户审批时暂停后继续；这增加了任务编排与幂等设计成本，但避免请求超时、重复副作用和不可恢复的半完成状态。

## Considered Options

- 同步请求：实现简单，但无法可靠承载长时间、多步骤和人工审批流程。
- 持久化运行：需要队列和状态管理，但能支持生产级 Agent 生命周期。
