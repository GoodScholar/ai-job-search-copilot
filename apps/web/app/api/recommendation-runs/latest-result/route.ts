import { api } from "@/lib/server/api-client"; import { readSessionToken } from "@/lib/server/session-cookie";
const h = { "Cache-Control": "no-store" }; export async function GET() { const token = await readSessionToken(); if (!token) return new Response(null, { status: 401, headers: h }); try { return Response.json({ run: await api.getLatestPublishedRecommendationRun(token) }, { headers: h }); } catch (error) { const s = typeof error === "object" && error !== null && "status" in error && error.status === 401 ? 401 : 502; return new Response(null, { status: s, headers: h }); } }
