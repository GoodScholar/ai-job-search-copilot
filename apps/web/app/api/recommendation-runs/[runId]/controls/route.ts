import { ControlRecommendationRunCommandSchema } from "@job-copilot/contracts/recommendation-runs"; import { z } from "zod"; import { api } from "@/lib/server/api-client"; import { readSessionToken } from "@/lib/server/session-cookie";
const h = { "Cache-Control": "no-store" }; export async function POST(request: Request, { params }: { params: Promise<{ runId: string }> }) { const token = await readSessionToken(); const { runId } = await params; if (!token) return new Response(null, { status: 401, headers: h }); if (!z.uuid().safeParse(runId).success) return new Response(null, { status: 404, headers: h }); const command = ControlRecommendationRunCommandSchema.safeParse(await request.json().catch(() => null)); if (!command.success) return new Response(null, { status: 400, headers: h }); try { return Response.json(await api.controlRecommendationRun(token, runId, command.data), { headers: h }); } catch (error) { const s = typeof error === "object" && error !== null && "status" in error && [400, 401, 404, 409].includes(error.status as number) ? error.status as number : 502; return new Response(null, { status: s, headers: h }); } }
